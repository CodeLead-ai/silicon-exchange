/**
 * Typed mock data — owned by increment inc-2.
 *
 * Data constants (listings, utilization samples, reservations) live in
 * src/data/index.ts. This module renders nothing; it exists as the
 * owning module for the data capability so later increments can extend
 * it if a visible surface is needed.
 */
export function TypedMockData() {
  return null;
}
